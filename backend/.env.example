SECRET_KEY=your-secret-key-for-development
DEBUG=True
DATA_FOLDER=data

# Firebase Configuration
# Option 1: Set the path to your Firebase credentials file
GOOGLE_APPLICATION_CREDENTIALS=path/to/your/firebase-credentials.json

# Option 2: Or paste the entire JSON content of your Firebase credentials
# FIREBASE_CREDENTIALS_JSON={"type": "service_account", "project_id": "your-project-id", ...}

# MongoDB Configuration
# For MongoDB Atlas, use a connection string like:
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/?retryWrites=true&w=majority
MONGODB_DB_NAME=tech_trends_db

# For local development:
# MONGODB_URI=mongodb://localhost:27017/
# MONGODB_DB_NAME=tech_trends_db 